# ============================================================
# CALVE Voice Agent — Production Requirements (Render)
# ============================================================
# Used by Render Build Command: pip install -r requirements.txt
# Audio/ML libs are intentionally excluded (not available on
# Render free tier). Use requirements-local.txt locally.

# Web framework
fastapi==0.115.9
uvicorn[standard]==0.34.0

# Data validation
pydantic==2.11.1

# Environment
python-dotenv==1.1.0

# Database
supabase==2.13.0

# AI / LLM
openai==1.70.0
langchain==0.3.21
langchain-openai==0.3.9
langchain-anthropic==0.3.10
langgraph==0.3.21
anthropic==0.49.0

# HTTP
requests==2.32.3

# Telephony (optional — only used when USE_TWILIO=true)
twilio==9.5.1
