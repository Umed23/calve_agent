# ============================================================
# CALVE Voice Agent — Production Requirements (Render)
# ============================================================
# Uses >= constraints so pip can pick pre-built wheels for
# whatever Python version Render provides (3.12+).
# Audio/ML libs excluded — use requirements-local.txt locally.

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data validation (no exact pin — lets pip pick a wheel)
pydantic>=2.7.0

# Environment
python-dotenv>=1.0.0

# Database
supabase>=2.10.0

# AI / LLM
openai>=1.50.0
langchain>=0.3.0
langchain-openai>=0.3.0
langchain-anthropic>=0.3.0
langgraph>=0.3.0
anthropic>=0.40.0

# HTTP
requests>=2.31.0

# Telephony (optional — only used when USE_TWILIO=true)
twilio>=9.0.0
