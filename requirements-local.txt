# ============================================================
# CALVE Voice Agent — Local Development Requirements
# ============================================================
# Extends production deps with audio + ML libraries for the
# local desktop voice agent (local_agent.py).
#
# Usage:  pip install -r requirements-local.txt

-r requirements.txt

# STT (Speech-To-Text)
faster-whisper
openai-whisper

# TTS / Audio processing
torch==2.0.1
torchaudio==2.0.2
sounddevice
soundfile
librosa

# Microphone input / VAD
pyaudio
webrtcvad

# Other audio utilities
scipy
numpy

# Optional: DeepFilterNet noise suppression
# deepfilternet
